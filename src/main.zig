const std = @import("std");


const Blockchain = struct {
    version: []const u8,                        // How else would we specifiy a string?
    author: []const u8,                         // How else would we specifiy a sting?


}

pub fn main() void {
    // Polka L1 Blockchain
    std.debug.print("Polka L1 Blockchain\n", .{});

    // Build the blockchain
    
}